"use client";

/**
 * Componente PWA Manager (stub)
 * Este componente existe para evitar errores de importación
 * Las funcionalidades PWA están completamente deshabilitadas
 */
export function PWAManager() {
  // Componente completamente vacío para evitar cualquier error
  // No se importa ni se usa ningún servicio que pueda causar problemas
  return null;
}

export default PWAManager;
